def main(x):
    def fib(n):
        def loop(a, b, n):
            if n == 0:
                a
            else: if n == 1:
                b
            else:
                loop(b, a + b, n - 1)
        in
        loop(0, 1, n)
    in
    fib(x)
